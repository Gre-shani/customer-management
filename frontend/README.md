# Customer Management Frontend

This is a minimal React frontend for the Customer Management System.

Run locally:

```powershell
cd frontend
npm install
npm start
```

The app expects the backend at `http://localhost:8081/api/customers`.
